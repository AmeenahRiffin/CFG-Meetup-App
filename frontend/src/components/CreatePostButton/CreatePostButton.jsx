import React from "react";
import "./CreatePostButton.css"; // Import the CSS file

const CreatePostButton = () => {
  return (
    <button className="create-post-btn" id="open-modal">
      Create a Post
    </button>
  );
};

export default CreatePostButton;
